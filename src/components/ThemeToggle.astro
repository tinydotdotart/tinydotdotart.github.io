<button 
  id="theme-toggle" 
  type="button"
  aria-label="Toggle Dark Mode"
  class="relative w-12 h-12 rounded-full bg-black/5 dark:bg-white/10 flex items-center justify-center border border-black/10 dark:border-white/20 hover:scale-110 transition-all duration-300 group overflow-hidden active:scale-95"
>
  <!-- MOON: Visible in Light Mode -->
  <svg 
    xmlns="http://www.w3.org/2000/svg" 
    class="w-5 h-5 block dark:hidden text-black -rotate-12 group-hover:rotate-0 transition-transform duration-500" 
    fill="none" 
    viewBox="0 0 24 24" 
    stroke="currentColor"
  >
    <path 
      stroke-linecap="round" 
      stroke-linejoin="round" 
      stroke-width="2" 
      d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" 
    />
  </svg>
  
  <!-- SUN: Visible in Dark Mode -->
  <svg 
    xmlns="http://www.w3.org/2000/svg" 
    class="w-5 h-5 hidden dark:block text-white rotate-0 group-hover:rotate-45 transition-transform duration-500" 
    fill="none" 
    viewBox="0 0 24 24" 
    stroke="currentColor"
  >
    <path 
      stroke-linecap="round" 
      stroke-linejoin="round" 
      stroke-width="2" 
      d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364-6.364l-.707.707M6.343 17.657l-.707.707m12.728 0l-.707-.707M6.343 6.343l-.707-.707M12 8a4 4 0 100 8 4 4 0 000-8z" 
    />
  </svg>

  <div class="absolute inset-0 bg-orange-500/0 dark:group-hover:bg-white/5 group-hover:bg-black/5 transition-colors duration-300"></div>
</button>

<script is:inline>
  function applyTheme() {
    const localStorageTheme = localStorage.getItem('theme');

    // Default to light mode for new visitors
    if (localStorageTheme === 'dark') {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }
  }

  function setupThemeToggle() {
    const toggleBtn = document.getElementById('theme-toggle');

    if (toggleBtn) {
      // Prevent duplicate event listeners during Astro page swaps
      const newBtn = toggleBtn.cloneNode(true);
      toggleBtn.replaceWith(newBtn);
      
      newBtn.addEventListener('click', () => {
        const isDark = document.documentElement.classList.toggle('dark');

        localStorage.setItem(
          'theme',
          isDark ? 'dark' : 'light'
        );
      });
    }
  }

  // Initial page load
  applyTheme();
  setupThemeToggle();

  // Astro View Transitions support
  document.addEventListener('astro:after-swap', () => {
    applyTheme();
    setupThemeToggle();
  });
</script>